import { IncreasepowerPipe } from './increasepower.pipe';

describe('IncreasepowerPipe', () => {
  it('create an instance', () => {
    const pipe = new IncreasepowerPipe();
    expect(pipe).toBeTruthy();
  });
});
